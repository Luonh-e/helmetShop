import BrandMenu from "../components/BrandMenu";
import Header from "../components/Header";

const Home = () => {
  return (
    <div>
      {<Header />}
      {<BrandMenu />}
    </div>
  );
};

export default Home;
